#
# FILE: {{ application }}.py
#
# DESCRIPTION: This DAG start and stop the '{{ application }}' application
#
# BEWARE: It is auto-generated by the DAG creator and therefore should
#   not be modified by hand
#

# BEWARE: DO NOT REMOVE THIS IMPORT (default heuristic looks for airflow imports)
from airflow.decorators import dag, task

definition = {
{% for application_detail in application_details %}
    {{ application_detail.type }} = ['{{ application_detail.value }}']
{% endfor %}
}

# dag_start_application('{{ region }}', '{{ environment }}', '{{ application }}', '{{ start_schedule }}', definition)
# dag_stop_application( '{{ region }}', '{{ environment }}', '{{ application }}', '{{ stop_schedule }}',  definition)
